//Script to check if any of the booleans inside the object are true which allows the level to be flipped
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class checkGravityCollision : MonoBehaviour
{
    public bool isActivated;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
